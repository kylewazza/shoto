{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "crons": [
    {
      "path": "/api/reveal-check",
      "schedule": "*/5 * * * *"
    }
  ]
}